from setuptools import setup, find_packages

setup(
    name="RL_Maze_Solver",
    version="0.1.0",
    author="AJO",
    packages=find_packages(),
    python_requires='>=3.6',
    tests_require=['pytest']
)
